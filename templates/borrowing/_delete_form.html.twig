<form method="post" action="{{ path('app_borrowing_delete', {'id': borrowing.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ borrowing.id) }}">

        <button class="btn">Supprimer</button>


</form>
