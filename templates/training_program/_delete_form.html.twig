<form method="post" action="{{ path('app_training_program_delete', {'id': training_program.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_program.id) }}">
    <button class="btn">Delete</button>
</form>
