{% extends 'base.html.twig' %}

{% block title %}TrainingProgram{% endblock %}
 {% block headTitle %}Détail d'une formation{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training_program.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ training_program.name }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ training_program.level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_training_program_index') }}">back to list</a>

    <a href="{{ path('app_training_program_edit', {'id': training_program.id}) }}">edit</a>

    {{ include('training_program/_delete_form.html.twig') }}
{% endblock %}
